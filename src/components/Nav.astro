---
import ThemeToggle from './ThemeToggle.astro';

const links = [
  { href: '/#work', label: 'Work' },
  { href: '/#experience', label: 'Experience' },
  { href: '/#contact', label: 'Contact' },
];
---

<header class="sticky top-0 z-50 bg-page/90 backdrop-blur">
  <nav class="max-w-content mx-auto flex items-center justify-between px-6 py-5">
    <a href="/#top" class="font-display font-semibold text-base text-ink">
      Omar Daniel.
    </a>
    <div class="flex items-center gap-6">
      <ul class="hidden sm:flex gap-6 font-body font-bold text-sm text-muted">
        {links.map((link) => (
          <li>
            <a href={link.href} class="hover:text-ink transition-colors">{link.label}</a>
          </li>
        ))}
      </ul>
      <ThemeToggle />
    </div>
  </nav>
</header>
