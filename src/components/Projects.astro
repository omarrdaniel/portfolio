---
import { getCollection } from 'astro:content';
import ProjectCard from './ProjectCard.astro';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
---

<section id="projects" class="section-divider">
  <div class="max-w-content mx-auto px-6 py-16">
    <span class="chip bg-chip-amber text-chip-amber-ink">Case studies</span>
    <h2 class="font-display text-2xl font-semibold mt-5 mb-2">Selected work.</h2>
    <p class="text-muted mb-8 max-w-xl">
      Client names are withheld under confidentiality agreements — architecture,
      approach, and outcomes are described accurately.
    </p>

    <div class="grid md:grid-cols-2 gap-6">
      {projects.map((project) => (
        <ProjectCard
          title={project.data.title}
          sector={project.data.sector}
          stack={project.data.stack}
          summary={project.data.summary}
          slug={project.id}
        />
      ))}
    </div>
  </div>
</section>
